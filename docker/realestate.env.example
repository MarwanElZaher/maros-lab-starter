# Real-estate stack environment — copy to docker/.env.realestate and fill values.
# Place at: /home/marwanelzaher/ai-company/maros-lab-starter/docker/.env.realestate
# chmod 600 docker/.env.realestate

# ── OpenRouter (LLM for query parsing) ───────────────────────────────────────
OPENROUTER_API_KEY=your_openrouter_key_here
